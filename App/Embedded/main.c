#include <stdio.h>
#include "Calc.h"

int main() {
    printf("Hello, Embedded!\n");
    printf("add(Embedded):%d\n",add(10,2));
    printf("sub(Embedded):%d\n",sub(10,2));
    printf("mul(Embedded):%d\n",mul(10,2));
    return 0;
}
